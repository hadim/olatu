# 0008 — Tides (marées)

- **Status:** Accepted
- **Date:** 2026-07-05
- **Authors:** Hadrien Mary (+ Claude)
- **Relates to:** [0001 §6 feature spec](2026-06-27-0001-foundation.md) (extends it —
  tides were a non-goal there), [0004 realtime scraper](2026-06-27-0004-realtime-scraper.md)
  (the forward-growing-accumulator pattern this reuses), [0005 multi-buoy](2026-06-27-0005-multi-buoy.md).

> Add **tide (marée)** information to Olatu, in both the real-time banner and the
> time-series charts, so a surfer/sailor can read at a glance: the next high/low tide
> and countdown, where we are in the cycle (rising/falling, near high/low), and the
> **marnage** (tidal range) — is it a big tide or not. Reference studied and partly
> ported: the sibling project `wave-monitor` (`scripts/sync-tides.ts`, `src/lib/tides.ts`).

---

## 1. Why

Wave height/period/direction is only half of what the sea is doing. Tide state drives
where and when it's surfable, and on this coast people think in tides constantly. Olatu
already answers "how big, from where, how warm"; this adds "and the tide is doing X".

Foundation §1 listed *forecasting* as a non-goal. Astronomical **tide predictions** are a
deliberate, bounded exception the owner requested — they are deterministic ephemeris, not
a wave/weather model, and are a standard, expected marine-page feature.

## 2. Decisions

1. **Source = api-maree.fr** (`/water-levels`), the same API `wave-monitor` uses. Upstream:
   **IFREMER / PREVIMER** harmonic components (via Sextant), **CC-BY**. Auth is a 32-hex
   `key` query param. Free tier: window **J−30 … J+30**, `step` ∈ {1,5,10,15,20,30,60} min,
   **≤ 1500 points/request**, **360 requests/hour**. There is **no** high/low or coefficient
   endpoint → we derive extrema ourselves.
   - *Consequence:* tides exist only for a **rolling ~±30-day window** (extended forward by
     a forward-growing accumulator, see §3). History older than that has no tide data →
     the UI shows an empty-state, exactly like sea-temperature already does before its
     accumulation start.
2. **"Big tide" framing = marnage in metres only** — no French *coefficient de marée*. A
   neap↔spring **metre** fill bar answers "big or small" without any Brest/coefficient math.
3. **Charts = a dedicated, synced tide panel** plotting **marnage (tidal range, m) over
   time** on the Y axis — a slowly-varying spring↔neap signal that reads at any zoom and
   accumulates with the tide history. *(Revised 2026-07-05: the first cut plotted the raw
   water-level curve with ▲/▼ PM/BM markers, capped to ≲30-day windows; that was unreadable
   past a few days and hid the accumulating history. The live water-level phase now lives
   only in the banner arc; the chart is marnage-over-time, no span cap.)*
4. **Storage = the HF bucket `hadim/olatu`** (like every other tier). The published tier is
   `<campaign>/data/tides.json`; the raw extrema **accumulator** is
   `<campaign>/raw/<campaign>_tides.csv`, forward-growing, mirroring the reel.

**Key handling.** The API key is **ingest-only** and never enters the client bundle — the
webapp only ever reads the pre-derived static `tides.json`. In CI the key is the GitHub
secret `API_MAREE_KEY` (a step back from keyless OIDC, but confined to the data job and
never shipped). Locally it comes from the env. A missing key is **non-fatal**: the tide
step is skipped and the rest of the refresh proceeds (keyless dev still builds).

## 3. Data model & pipeline

New ingest step `ingest/tides.py`, run from `update.py` **after scrape, before build**:

1. **Fetch** `/water-levels` for the buoy's `tide_site` over **J−30 … J+30** at `step=10`
   min, in **10-day chunks** (1440 pts < 1500 cap), `tz=UTC` (so returned times are UTC —
   Olatu's storage convention; the webapp renders Europe/Paris as usual). De-dup the seam
   point shared between chunks.
2. **Derive extrema** (port of `sync-tides.ts findExtrema`): a **W-sample slope test**
   (W=3 ≈ 30 min at step=10) robust to the API's 3-decimal plateaus near peaks, then a
   **parabolic sub-sample** of the exact time/height through the three neighbours, then a
   same-kind de-dup within 90 min. Output: `(datetime_utc, height_m, kind∈{high,low})`.
3. **Coalesce** into `raw/<campaign>_tides.csv` with a **replace-window merge**: keep old
   events *outside* the freshly-fetched [J−30, J+30] window, replace everything *inside*
   with the new extrema. Consecutive fetch windows always overlap (60-day windows, gated
   ≈ every 10 days), so no gap forms and the region that scrolls out of each new window is
   frozen into permanent forward-growing history. Self-healing for the recent past.
4. **Emit** `data/tides.json`:
   ```json
   {
     "site": "socoa", "site_label": "Socoa (Saint-Jean-de-Luz)",
     "timezone": "Europe/Paris",
     "generated_at": "2026-07-05T…Z",
     "range_ref": { "neap": 1.2, "spring": 4.5 },
     "source": { "provider": "api-maree.fr", "upstream": "IFREMER / PREVIMER",
                 "license": "CC-BY", "credit": "Hauteurs d'eau diffusées par api-maree.fr,
                 calculées à partir de composantes harmoniques IFREMER / PREVIMER.",
                 "url": "https://api-maree.fr" },
     "events": [ { "t": 1720000000, "h": 0.84, "k": "low" }, … ]
   }
   ```
   `t` = **epoch seconds UTC** (matches the columnar tiers' `t`); `h` = metres; `k` =
   `high|low`. `build.py` adds `"tides": "tides.json"` to `manifest.tiers`.

**Fetch cadence / gate.** Only fetch when the accumulator's **forward horizon < 20 days**
(stateless, quota-polite): after a fetch we hold +30 days, so it re-fires ≈ every 10 days
per buoy — 3 buoys × ~6 chunked requests ≪ 360/h. The accumulator always brackets "now",
so the banner is always populated between fetches.

**Per-buoy config** in `ingest/schema.py` `BUOYS`:
- `tide_site` — the api-maree.fr site id. **To validate against `/sites`** once a key
  exists (best guesses: 06403 → `socoa`, 06402 → `boucau-bayonne-biarritz`,
  03302 → `arcachon-eyrac`). A bad/absent site → fetch fails → empty-state, not a crash.
- `tide_range_ref {neap, spring}` (metres) — the neap→spring envelope for the magnitude
  bar. Regional constants (Basque ≈ {1.2, 4.5} like wave-monitor; Arcachon ≈ {1.3, 4.6}),
  **not** derived from the J±30 window (one spring-neap cycle would mislabel a monthly max).

**Published-tier size.** `tides.json` publishes the full accumulator; it starts at ~J±30
(≈ 240 events, ~8 KB) and grows ~1460 events/yr (~15 KB/yr). Revisit a rolling cap
(e.g. last N years) if it ever gets heavy — a one-line change, no format break.

## 4. Runtime (webapp)

`webapp/src/lib/tides.ts` — a **framework-agnostic** port of wave-monitor's pure functions:
- `parseTides(json)` → `{ meta, rangeRef, events: {t(ms), h, kind}[] }`.
- `tidePhase(events, now)` → previous/next extremum, `rising`, `progress` (fraction of the
  half-cycle), interpolated `height` (**raised-cosine** `h = prev + (next−prev)·(1−cos(π·p))/2`),
  `amplitude` (marnage of the current half-cycle), `msToNext`, coarse `label`
  (near-low / rising / near-high / falling). `null` when `now` isn't bracketed.
- `reconstructCurve(events, xmin, xmax, stepSec)` → samples the raised-cosine between
  consecutive extrema for the chart panel.
- `tideMagnitude(range, ref)` → `{ t∈[0,1], label }` in **metres** (no coefficient).

**Reconstruction is the raised-cosine** (the theoretical half-sine tide shape), *not* the
literal predicted curve — nearshore shallow-water asymmetry (Adour, Arcachon lagoon) is not
modelled. Documented simplification; acceptable for these regular semi-diurnal stations.
Ship extrema only (light + accumulates); reconstruct in the browser.

### 4.1 Banner (`TideStrip` in `CurrentConditions`)
A compact, single responsive band at the bottom of the current-conditions card (hairline-
separated; stacks on mobile):
- a **half-sine arc with a marker riding it** (rising/falling + where-in-cycle — the
  wave-monitor signature, in Olatu tokens),
- the **phase** word (Rising/Falling/Near high/Near low → montante/descendante/étale),
- the **next** extremum with a **live countdown** ("Next ▲ 14:32 · in 2 h 08", ticked by
  `useNow`) and the previous one,
- **marnage** in metres + a **neap↔spring metre fill bar**.
Empty-state (no tide data / `now` unbracketed): the strip hides gracefully.

### 4.2 Chart (dedicated tide panel in `TimeSeries`)
A new synced uPlot panel, special-cased because its series is **external** (built from
`events`, not the wave `Columnar`):
- Y = **marnage** (tidal range, m): one point per extremum valued at the |Δheight| to the
  previous one (`marnageSeries`), a **zero-based** filled line. A > 12 h gap in the extrema
  breaks the line. Smoothed by the Raw/Light/Strong control like the wave panels.
- shared x-axis + crosshair; the hover card + accessible summary show the marnage of the
  bracketing cycle (`marnageAt`); an **empty-state overlay** (`chart-empty` + `emptyKey`)
  only where the window has no tide data — **no span cap**, so the full accumulating history
  shows like the other panels. The live water-level *phase* lives in the banner, not here.

## 5. i18n & attribution

- New `tide_*` message keys in `messages/{en,fr,es}.json` (title, montante/descendante/
  étale/near-high/near-low, next high/low = PM/BM, "in", marnage, neap↔spring words,
  `tide_unavailable`, a glossary entry). Keep the three files in **key-parity**.
- **Attribution** (CC-BY, required): the credit string above, surfaced in the unified
  data-source area (footer + glossary), alongside CANDHIS/HF/GitHub. Re-check the exact
  wording in api-maree.fr's CGU before launch.

## 6. Verification

- **Ingest:** `API_MAREE_KEY=… pixi run update --campaign 06403 --no-upload`; inspect
  `hfdata/06403/data/tides.json` — events sorted, **alternating** high/low, metres, PM/BM
  times within a few min of maree.info (Socoa). Confirm the horizon gate skips a same-day
  re-run. Repeat 06402/03302; verify a bad site degrades to skip, not crash. Offline unit
  check of `findExtrema` on a synthetic cosine series.
- **Webapp:** banner strip shows correct rising/falling + next PM/BM + live countdown +
  marnage; chart tide panel draws the curve with ▲/▼ on the default window and empty-states
  on a 2018 window; buoy switch never cross-pairs; FR/ES copy; light+dark; mobile; a11y.
- **Security:** `grep -r` the built `webapp/dist` to assert the key never appears.

## 7. Open items

1. ~~Confirm api-maree.fr **site ids** via `/sites`~~ — done (revision 2026-07-05, see §8);
   also re-check the exact CGU attribution wording before launch.
2. Tune `tide_range_ref` per port once real extrema are observed.
3. Owner: create an api-maree.fr account; add the `API_MAREE_KEY` GitHub secret.

## 8. Revision 2026-07-05 — port-keyed storage, curve chart, sun times

Owner feedback after the first cut shipped. Five changes; two of them revise §3/§4.3
decisions above.

### 8.1 Site ids were wrong → only Anglet had tides

The `tide_site` guesses in §3 were **best-effort and 2/3 were invalid** — they don't
exist in api-maree.fr `/sites` (125 sites, queried with the owner's key):

| buoy  | was (§3)                  | valid? | corrected (nearest real site) | dist  |
|-------|---------------------------|--------|-------------------------------|-------|
| 06403 | `socoa`                   | ❌      | `saint-jean-de-luz`           | 0.8 km |
| 06402 | `boucau-bayonne-biarritz` | ✅      | `boucau-bayonne-biarritz`     | 5.9 km |
| 03302 | `arcachon-eyrac`          | ❌      | `cap-ferret`                  | 16.5 km |

Only Anglet's guess happened to be valid, so only Anglet ever produced a `tides.json`.

### 8.2 Storage split: tides are a **port** resource, keyed at the bucket root (Parquet)

**Revises decision 4.** Tides are a property of a **port/site**, not a buoy — two nearby
buoys can share one port, and the tide station is a distinct place from the wave buoy. So
tide data moves *out* of the per-campaign prefix to a shared, port-keyed root:

```
tides/<port>/raw/extrema.csv     forward-growing high/low accumulator (was <campaign>/raw/<c>_tides.csv)
tides/<port>/data/tides.parquet  published tier (was <campaign>/data/tides.json)
```

- **Parquet, not JSON** (owner choice) — same columnar convention as the wave tiers
  (Snappy), read in the browser via hyparquet like `daily.parquet`. Columns: `t` (Int64
  epoch s, UTC), `h` (Float64 m), `k` (Utf8 `high|low`). Extrema are tiny (~1460/yr/port)
  so a single row group is fine; the CI multi-row-group assertion is relaxed for this tier.
- **Nearest-port resolution + threshold.** `ingest/schema.py` gains a curated `TIDE_PORTS`
  registry (`id → {label, lat, lon, range_ref}`) for the ports we actually fetch. Each buoy
  resolves to its nearest port via `resolve_tide_port(campaign)`; **beyond `TIDE_MAX_KM`
  (40 km) → no port → the buoy shows the tide empty-state** ("marée indisponible"), exactly
  the model the owner asked for. `tide_site`/`tide_site_label`/`tide_range_ref` leave the
  per-buoy `BUOYS` table (they were port properties).
- **Manifest carries the resolution.** `build.py` writes a `tide` block into each buoy's
  `manifest.json` — `{ port, label, distance_km, range_ref, source }` — or `null` when no
  port is within threshold. The webapp reads tide meta from the (already-loaded) manifest
  and fetches only the shared `tides/<port>/data/tides.parquet` for the events (via a new
  `tidesBase(port)` = `DATA_ROOT + tides/<port>/data/`, a bucket-root path, *not*
  campaign-relative). The old `tiers.tides` pointer and per-campaign `tides.json` are
  dropped (orphaned copies on the bucket are harmless; the port accumulator re-fetches
  J±30 on first run).
- **Ingest/upload.** `update.py`'s tide step resolves the campaign's port, pulls
  `tides/<port>/raw`, refreshes it, and uploads `tides/<port>/`. Fetch is still gated by
  the forward-horizon check. (Fetch-dedup across a *shared* port — two buoys, one port —
  would want a standalone tide job; today all three buoys map to distinct ports, so the
  per-campaign path is correct and simplest. Noted for when a shared port appears.)

### 8.3 Chart: **water-level curve + PM/BM markers**, Y auto (revises §4.2 / decision 3)

The marnage-over-time panel (decision 3) was abstract — it never showed *when* high/low
tide is, which is the thing a surfer actually reads. **Revert to the tide curve**, done
right this time:

- **Series = the reconstructed water level** (raised-cosine between extrema, `reconstructCurve`
  in `lib/tides.ts`) with **▲ high / ▼ low markers** at the extrema (a canvas draw hook,
  density-thinned like the direction glyphs). **Adaptive** so it reads at every zoom:
  windows ≤ ~21 d reconstruct the smooth 10-min curve; wider windows connect the extrema
  directly (a cheap zig-zag whose upper/lower edges are the PM/BM envelope — undersampling
  a raised-cosine would alias, so we don't). A > 12 h gap in the extrema breaks the line.
- **Y is auto min/max** (not zero-based) — water level sits well above chart datum, so a
  min/max frame gives the oscillation real vertical resolution.
- **Hover** shows the reconstructed water level (m) at the cursor; the **accessible summary**
  shows the window's lowest low / highest high / current level (marnage stays in the banner,
  the "big tide?" home). Empty-state overlay only where the window has no extrema — no span cap.

### 8.4 Banner polish

- **`TideStrip` fill bug** — the arc's under-fill was `` `${hue}22` `` with `hue` a literal
  `var(--accent)`, i.e. `var(--accent)22`, invalid CSS → SVG `fill` fell back to **black**
  ("tout noir sous la courbe"). Fixed with `fillOpacity` (theme-safe).
- **Previous extremum** shown next to the next one ("Last ▼ 08:14 · Next ▲ 14:32 · in 2 h").
- **Sunrise / sunset** — a pure client-side NOAA solar calc (`lib/sun.ts`, lat/lon/date →
  UTC instants, no API, no key), rendered in the buoy tz. Nice-to-have the owner flagged
  "only if easy" — it is.

### 8.5 Verification addendum

Regenerate all three campaigns locally with the key (corrected ids), serve `hfdata` to the
webapp (`VITE_DATA_BASE_URL`), and confirm: tides on **all three** buoys, curve + PM/BM
markers on narrow windows and the envelope zig-zag on wide ones, min/max Y, prev+next in the
banner, sun times, no black fill, buoy switch never cross-pairs, FR/ES, light+dark, a11y.

## 9. Revision 2026-07-05 — banner strip UX (timeline + separated sun)

Owner feedback on the shipped `TideStrip` (§8.4): the band had grown dense, and two things
weren't intuitive — (a) sunrise/sunset sat *between* the tide facts, and (b) it wasn't clear
that the left time was the **previous** tide and the right the **upcoming** one. Reworked into
two clearly separated groups (no data dropped — still shows everything, just legibly grouped):

- **The square half-sine arc became an integrated tide-CURVE timeline.** The reconstructed
  raised-cosine is drawn between the previous extremum (**left**) and the next (**right**),
  with a pulsing **"now" dot riding the curve**. Past is literally to the left, future to the
  right — the ambiguity is gone. The previous endpoint is a **hollow/muted** dot ("where we
  came from"), the next a **filled accent** dot ("where we're heading"); each endpoint carries
  its kind (▼ low / ▲ high) + time as a caption hugging its side, and the next side also shows
  the live countdown. The marnage + neap↔spring fill stays beside it, all inside the **TIDE**
  group.
- **Sun is its own titled zone.** Sunrise/sunset moved out from between the tide facts into a
  separate **SUN** group behind a divider (its own label + glyphs), so it no longer reads as
  tide data. Stacks below the tide group on mobile.
- New i18n keys (`tide_previous`/`tide_next`/`tide_now`/`sun_title`, EN/FR/ES parity); the
  curve carries an `aria-label` summarising phase + prev/next + countdown.

Reconstruction, extrema source, empty-state and the chart panel (§8.3) are unchanged.

## 10. Revision 2026-08-01 — the tide calendar (day-by-day predictions)

Owner request: *"à côté de la marée en cours, un petit bouton calendrier qui, quand on clique,
montre un calendrier pour les jours avant et suivants — en fonction des données disponibles —
avec les marées à venir."*

The banner answers **"what is the tide doing now"**; nothing answered **"and tomorrow at
sunrise / last Saturday"** — yet the extrema needed to answer it were already loaded (the whole
port accumulator ships in `tides.parquet`; §8.2). So this is pure UI over data already in hand:
**no ingest, no new fetch, no new tier.**

### 10.1 Shape

A quiet calendar button sits **next to the live phase word** in the TIDE group; it opens a
Radix popover (spec 0006 §4: focus return, Esc, outside-click) split in two:

- **A month grid** (Monday-first, one month at a time, `‹ ›` bounded by the data). Each day
  carries a **marnage bar** — `tideMagnitude(day.marnage, range_ref)` as a fill width, warm
  above `large` exactly like the banner's hue rule — so the **spring↔neap rhythm of the month
  reads at a glance** and you can pick a big-tide day without opening it. Today keeps an accent
  ring; the selected day fills accent.
- **The selected day's tides**: every extremum as `▲/▼ · kind · time · height`, plus the day's
  marnage + magnitude word. Extrema already past are dimmed, and **the next one overall**
  (the very one the banner counts down to) is highlighted with its live countdown.

A footer states the **coverage** (`first – last` day held), so an empty future is legible as
"predictions stop here", not as a bug.

### 10.2 Decisions

1. **Days are the buoy's local calendar days, not UTC days.** A 23:40 UTC high tide is a
   *next-day* tide in Europe/Paris; grouping on UTC would file it under the wrong date and shift
   a fifth extremum into the wrong cell. `zonedDayIndex(ms, tz)` (new, `lib/tides.ts`) formats
   through `Intl` in the buoy zone and returns days-since-epoch; `groupTidesByDay` buckets on it.
   The index doubles as that date's UTC midnight, so labels format with `timeZone: 'UTC'`.
2. **Bounds come from the data, never from a rule.** The selectable range is exactly
   `[first, last]` day present in the accumulator — today ≈ J−(everything ingest ever saw) to
   ≈ J+30 (§3 horizon gate), and it grows on its own as the accumulator does. Days with no
   extrema are disabled, so the calendar can never offer a day it has no tides for. Opening it
   always lands on **today** (or the nearest covered day if the predictions have gone stale).
3. **Per-day marnage = the biggest half-cycle ending that day**, and it is `null` across a hole
   wider than 12 h — a gap in the predictions must not invent a marnage spanning it.
4. **No new data, no new state to persist.** The selected day is popover-local: this is a
   lookup, not a mode, and it must not compete with the chart's own date range (`DatePicker`).

### 10.3 Touch list

- **`lib/calendar.ts`** (new) — the Monday-first month-grid helpers (`monthCells`, `monthOf`,
  `monthKey`, `weekdayNarrow`, `monthLabel`), now shared with the chart-range `DatePicker`
  (which had its own copy).
- **`lib/tides.ts`** — `zonedDayIndex` / `zonedDayFormatter` / `groupTidesByDay` + `TideDay`.
- **`components/TideCalendar.tsx`** (new); **`TideStrip.tsx`** mounts it beside the phase word.
- **`lib/format.ts`** — `fmtCountdown` moved out of `TideStrip` (both surfaces show it now).
- **`icons.tsx`** — a shared `calendar` glyph (`DatePicker` dropped its inline copy).
- **`messages/{en,fr,es}.json`** — `tide_calendar`, `tide_today`, `tide_coverage`.

### 10.4 Verification

Live against the bucket, buoy 06403: the grid shows the covered window (2026-06-05 → 08-22 on
the day of writing) with the tail of August disabled; the spring/neap bars alternate on the
~14-day beat; the selected day lists 4 extrema matching the banner's prev/next; the next-tide
countdown agrees with the banner's to the minute. FR/ES (`Samedi 1 août` — first-letter case,
**not** `capitalize`, which would title-case the month), light + dark, and the ≤560 px stacked
layout all check out.
